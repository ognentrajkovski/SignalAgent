version: '3.8'

services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: signaluser
      POSTGRES_PASSWORD: signalpassword
      POSTGRES_DB: signaldb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U signaluser -d signaldb"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
